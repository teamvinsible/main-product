FROM node:22-bookworm-slim

WORKDIR /app

# Lightweight runtime server for agent builds inside CF Containers
COPY runtime/package.json ./package.json
RUN npm install --omit=dev

COPY runtime/server.mjs ./server.mjs

ENV PORT=8080
EXPOSE 8080

CMD ["node", "server.mjs"]
